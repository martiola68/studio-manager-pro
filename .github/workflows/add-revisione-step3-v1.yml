name: Add revisione step3 v1

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/add-revisione-step3-v1.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Patch Step 3 independence questionnaire
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('src/pages/revisione-controllo/presa-in-carico.tsx')
          s = p.read_text()

          marker = '] as const;\n\ntype Risposta = "" | "SI" | "NO" | "NON_APPLICABILE";'
          if marker not in s:
              raise SystemExit('marker domande Step2 non trovato')

          step3 = r'''
] as const;

const domandeStep3 = [
  ["1", "Rapporti e situazioni personali", "Il revisore è domiciliato nei locali del professionista incaricato della tenuta della contabilità o che svolge attività di consulenza per la Società?", "Descrivere l'eventuale rapporto logistico o professionale e valutarne l'impatto sull'indipendenza."],
  ["2", "Rapporti e situazioni personali", "Presso lo studio del revisore è domiciliata la Società oggetto di controllo?", "Indicare l'eventuale domiciliazione e le misure adottate per salvaguardare l'indipendenza."],
  ["3", "Rapporti e situazioni personali", "Il revisore ha un contenzioso con la Società oggetto di controllo?", "Descrivere natura, stato e rilevanza del contenzioso."],
  ["4", "Rapporti e situazioni personali", "Presso il revisore opera, come dipendente, collaboratore o praticante, una persona in grado di influenzare il risultato della revisione che lavora contemporaneamente come dipendente o collaboratore della Società o di una sua consociata?", "Specificare soggetti coinvolti, ruolo e misure di salvaguardia."],
  ["5", "Rapporti e situazioni personali", "Il revisore effettua un distacco di personale alla Società o ad una sua consociata?", "Indicare personale, durata, attività svolta e valutazione della minaccia."],

  ["6", "Altri incarichi", "Il revisore è membro dell'organo di amministrazione della Società?", "Specificare l'eventuale carica ricoperta."],
  ["7", "Altri incarichi", "Il revisore è membro dell'organo di amministrazione di un'entità che controlla la Società o di un'entità che la Società controlla?", "Indicare entità e carica ricoperta."],
  ["8", "Altri incarichi", "Il professionista è sindaco unico di un'entità che controlla la Società o di un'entità che la Società controlla?", "Indicare entità e incarico."],
  ["9", "Altri incarichi", "Il professionista è membro del Collegio Sindacale di un'entità che controlla la Società o di un'entità che la Società controlla?", "Indicare entità e incarico."],
  ["10", "Altri incarichi", "Il professionista è revisore di un'entità che controlla la Società o di un'entità che la Società controlla?", "Indicare entità e incarico."],
  ["11", "Altri incarichi", "Il revisore ha rivestito nel passato la carica di amministratore o membro della direzione aziendale della Società?", "Indicare carica e periodo interessato."],
  ["12", "Altri incarichi", "Il revisore svolge presso la stessa Società anche un incarico di consulenza?", "Descrivere natura, estensione e compenso della consulenza."],
  ["13", "Altri incarichi", "Il revisore presta servizi in materia di organizzazione aziendale diretti alla selezione, formazione e gestione del personale?", "Descrivere i servizi prestati e il grado di coinvolgimento decisionale."],
  ["14", "Altri incarichi", "Il revisore predispone le registrazioni contabili e il bilancio della Società?", "Descrivere le attività contabili eventualmente svolte."],
  ["15", "Altri incarichi", "Il revisore assiste la Società nella determinazione o modifica delle scritture contabili o nella classificazione dei conti o operazioni senza la previa autorizzazione della direzione aziendale?", "Specificare attività e responsabilità della direzione."],
  ["16", "Altri incarichi", "Il revisore presta assistenza di natura esclusivamente tecnica o consulenza di natura puramente informativa?", "Descrivere l'assistenza prestata e i limiti dell'intervento."],
  ["17", "Altri incarichi", "Il revisore prepara documenti giustificativi o crea dati?", "Indicare i documenti o dati predisposti e le responsabilità mantenute dal cliente."],
  ["18", "Altri incarichi", "Il revisore esegue compiti contabili meccanici quali la registrazione di operazioni per le quali la direzione del soggetto sottoposto a revisione ha determinato la classificazione contabile da applicare?", "Descrivere i compiti contabili e le autorizzazioni ricevute."],
  ["19", "Altri incarichi", "Il revisore fornisce il servizio di elaborazione dati tramite una Società di cui è socio?", "Indicare società, natura dei servizi e rapporti economici."],
  ["20", "Altri incarichi", "Il revisore fa parte di uno Studio Associato che fornisce servizi di elaborazione dati o di consulenza alla Società?", "Descrivere i servizi resi dallo Studio Associato e le salvaguardie adottate."],
  ["21", "Altri incarichi", "Il revisore progetta e realizza sistemi informativi-contabili o amministrativi-finanziari per la Società?", "Descrivere sistemi, ruolo del revisore e responsabilità del cliente."],
  ["22", "Altri incarichi", "Il revisore svolge per la Società un servizio di software selection?", "Descrivere il servizio e il livello di coinvolgimento nelle decisioni."],
  ["23", "Altri incarichi", "Il revisore svolge funzione di Organismo di Vigilanza ai sensi del D.Lgs. 231?", "Indicare ruolo e perimetro dell'incarico."],
  ["24", "Altri incarichi", "Il revisore formula suggerimenti relativi alla definizione delle macro caratteristiche del Modello di Organizzazione ex D.Lgs. 231/2001?", "Descrivere il supporto fornito e le decisioni riservate al cliente."],
  ["25", "Altri incarichi", "Il revisore svolge attività di due diligence per conto della Società?", "Indicare oggetto, periodo e destinatari della due diligence."],
  ["26", "Altri incarichi", "Il revisore presta servizi di valutazione e stime attuariali?", "Descrivere le valutazioni effettuate e la loro rilevanza per il bilancio."],
  ["27", "Altri incarichi", "Il revisore fornisce supporto metodologico per la pianificazione di progetti e project management?", "Descrivere il supporto e le responsabilità operative del cliente."],
  ["28", "Altri incarichi", "Il revisore predispone perizie per conto della Società o per società del gruppo?", "Indicare oggetto e finalità delle perizie."],
  ["29", "Altri incarichi", "Il revisore esegue prestazioni di internal audit?", "Descrivere le prestazioni e la separazione delle responsabilità."],
  ["30", "Altri incarichi", "Il revisore prende decisioni in merito al framework del sistema di controllo interno da utilizzare, ai controlli e alle procedure da implementare?", "Descrivere eventuali decisioni o raccomandazioni e chi ne assume la responsabilità."],
  ["31", "Altri incarichi", "Il revisore gestisce esternamente i servizi di controllo interno o svolge un'attività ricorrente di monitoraggio del sistema di controllo interno?", "Descrivere natura e continuità del servizio."],
  ["32", "Altri incarichi", "Il revisore partecipa ad attività di analisi, mappatura e descrizione di processi, procedure e sistemi?", "Descrivere l'attività svolta e il livello di coinvolgimento."],
  ["33", "Altri incarichi", "Il revisore effettua Gap Analysis con indicazione delle aree che dovranno essere oggetto di intervento?", "Descrivere l'analisi e le raccomandazioni formulate."],
  ["34", "Altri incarichi", "Il revisore partecipa alla definizione dell'action plan relativo alle azioni da intraprendere in merito ai gap evidenziati in Gap Analysis svolte?", "Indicare ruolo del revisore e responsabilità della direzione."],
  ["35", "Altri incarichi", "Il revisore presta servizi di consulenza legale che comportino l'attribuzione di poteri di rappresentanza del cliente?", "Descrivere i servizi e gli eventuali poteri attribuiti."],
  ["36", "Altri incarichi", "Il revisore presta attività di patrocinatore legale o consulente tecnico di parte a favore del soggetto sottoposto a revisione, o di società del gruppo, nell'ambito di una controversia o lite?", "Descrivere controversia, ruolo e rilevanza economica."],
  ["37", "Altri incarichi", "Il revisore effettua attestazione dei costi?", "Indicare natura e finalità dell'attestazione."],

  ["38", "Rapporti economici", "Il revisore riceve un corrispettivo per la propria attività stabilito dal soggetto sottoposto a revisione in funzione dei risultati del servizio reso?", "Descrivere eventuali componenti variabili o contingent fee."],
  ["39", "Rapporti economici", "Il revisore è finanziariamente dipendente dalla Società?", "Valutare il peso economico del cliente sul totale dei compensi professionali."],
  ["40", "Rapporti economici", "Per gli incarichi in corso, trascorsi 12 mesi, il revisore non riceve i corrispettivi per le prestazioni rese?", "Indicare compensi scaduti, anzianità e iniziative intraprese."],
  ["41", "Rapporti economici", "Il revisore presenta rapporti patrimoniali o interessi finanziari nella Società sottoposta a revisione?", "Descrivere natura ed entità dei rapporti patrimoniali o interessi finanziari."],
  ["42", "Rapporti economici", "Il revisore detiene un interesse finanziario nella Società oggetto di controllo ovvero in altra entità in cui la Società ha effettuato un investimento?", "Specificare l'interesse finanziario e la sua rilevanza."],
  ["43", "Rapporti economici", "Il revisore collabora, anche in via occasionale, con i professionisti che svolgono attività di consulenza nei confronti della Società?", "Descrivere collaborazioni e possibili interferenze con l'indipendenza."],
  ["44", "Rapporti economici", "Il revisore instaura una relazione d'affari, acquisto o vendita, con la Società a condizioni di mercato?", "Indicare natura e importo delle operazioni."],
  ["45", "Rapporti economici", "Il revisore instaura una relazione d'affari, acquisto o vendita, con la Società NON a condizioni di mercato?", "Descrivere natura, importo e condizioni dell'operazione."],
  ["46", "Rapporti economici", "Il revisore riceve compensi eccessivamente contenuti rispetto alla prestazione resa?", "Documentare il criterio di determinazione del compenso e la sua adeguatezza."],

  ["47", "Minacce e salvaguardie", "È stata valutata la presenza di rischi derivanti da interesse personale?", "Descrivere le minacce di interesse personale eventualmente individuate."],
  ["48", "Minacce e salvaguardie", "Sono state predisposte misure di salvaguardia tali da eliminare o ridurre ad un livello accettabile i rischi derivanti da interesse personale?", "Indicare le misure di salvaguardia adottate."],
  ["49", "Minacce e salvaguardie", "È stata valutata la presenza di rischi derivanti da auto-riesame?", "Descrivere le minacce da auto-riesame eventualmente individuate."],
  ["50", "Minacce e salvaguardie", "Sono state predisposte misure di salvaguardia tali da eliminare o ridurre ad un livello accettabile i rischi derivanti da auto-riesame?", "Indicare le misure di salvaguardia adottate."],
  ["51", "Minacce e salvaguardie", "È stata valutata la presenza di rischi derivanti dalla promozione degli interessi del cliente?", "Descrivere le minacce di advocacy eventualmente individuate."],
  ["52", "Minacce e salvaguardie", "Sono state predisposte misure di salvaguardia tali da eliminare o ridurre ad un livello accettabile i rischi derivanti dalla promozione degli interessi del cliente?", "Indicare le misure di salvaguardia adottate."],
  ["53", "Minacce e salvaguardie", "È stata valutata la presenza di rischi derivanti da familiarità?", "Descrivere le minacce derivanti da familiarità."],
  ["54", "Minacce e salvaguardie", "Sono state predisposte misure di salvaguardia tali da eliminare o ridurre ad un livello accettabile i rischi derivanti da familiarità?", "Indicare le misure di salvaguardia adottate."],
  ["55", "Minacce e salvaguardie", "È stata valutata la presenza di rischi derivanti da intimidazione?", "Descrivere le minacce derivanti da intimidazione."],
  ["56", "Minacce e salvaguardie", "Sono state predisposte misure di salvaguardia tali da eliminare o ridurre ad un livello accettabile i rischi derivanti da intimidazione?", "Indicare le misure di salvaguardia adottate."],
  ["57", "Minacce e salvaguardie", "Ove presenti, è stata valutata l'indipendenza con riferimento agli enti controllati e agli enti consociati del cliente?", "Indicare gli enti considerati e l'esito della verifica."],
  ["58", "Minacce e salvaguardie", "Ove presente una struttura più ampia che costituisce una rete, è stata valutata l'indipendenza dei soggetti appartenenti a tale rete?", "Descrivere la rete e le verifiche effettuate."],
  ["59", "Minacce e salvaguardie", "Sono state predisposte le misure di salvaguardia tali da eliminare o ridurre ad un livello accettabile le minacce all'indipendenza?", "Riepilogare le salvaguardie complessive adottate."],
] as const;

type Risposta = "" | "SI" | "NO" | "NON_APPLICABILE";'''
          s = s.replace(marker, step3, 1)

          old = '  const [dataAccettazioneStep2, setDataAccettazioneStep2] = useState("");\n  const [loadingClienti, setLoadingClienti] = useState(false);'
          new = '''  const [dataAccettazioneStep2, setDataAccettazioneStep2] = useState("");
  const [risposteStep3, setRisposteStep3] = useState<Record<string, Riga>>({});
  const [conclusioniStep3, setConclusioniStep3] = useState("");
  const [rischioIndipendenza, setRischioIndipendenza] = useState("");
  const [esitoIndipendenza, setEsitoIndipendenza] = useState("");
  const [misureSalvaguardia, setMisureSalvaguardia] = useState("");
  const [compensoS, setCompensoS] = useState("");
  const [compensoC, setCompensoC] = useState("");
  const [compensoCT, setCompensoCT] = useState("");
  const [loadingClienti, setLoadingClienti] = useState(false);'''
          if old not in s: raise SystemExit('state marker non trovato')
          s = s.replace(old, new, 1)

          old = '  const completateStep2 = useMemo(() => domandeStep2.filter(([c]) => !!risposteStep2[c]?.risposta).length, [risposteStep2]);\n  const clienteSelezionato = useMemo(() => clienti.find((c) => c.id === clienteId) || null, [clienti, clienteId]);'
          new = '''  const completateStep2 = useMemo(() => domandeStep2.filter(([c]) => !!risposteStep2[c]?.risposta).length, [risposteStep2]);
  const completateStep3 = useMemo(() => domandeStep3.filter(([c]) => !!risposteStep3[c]?.risposta).length, [risposteStep3]);
  const rapportoSCCT = useMemo(() => {
    const sVal = Number(compensoS.replace(",", ".")) || 0;
    const cVal = Number(compensoC.replace(",", ".")) || 0;
    const ctVal = Number(compensoCT.replace(",", ".")) || 0;
    return ctVal > 0 ? ((sVal + cVal) / ctVal) * 100 : 0;
  }, [compensoS, compensoC, compensoCT]);
  const rapportoSsuSC = useMemo(() => {
    const sVal = Number(compensoS.replace(",", ".")) || 0;
    const cVal = Number(compensoC.replace(",", ".")) || 0;
    return sVal + cVal > 0 ? sVal / (sVal + cVal) : 0;
  }, [compensoS, compensoC]);
  const clienteSelezionato = useMemo(() => clienti.find((c) => c.id === clienteId) || null, [clienti, clienteId]);'''
          if old not in s: raise SystemExit('memo marker non trovato')
          s = s.replace(old, new, 1)

          old = '.in("codice", ["VALUTAZIONE_PRELIMINARE", "ACCETTAZIONE_INCARICO"]);'
          new = '.in("codice", ["VALUTAZIONE_PRELIMINARE", "ACCETTAZIONE_INCARICO", "VALUTAZIONE_INDIPENDENZA"]);'
          if old not in s: raise SystemExit('query marker non trovato')
          s = s.replace(old, new, 1)

          old = '''        if (q.codice === "ACCETTAZIONE_INCARICO" && String(r.codice_domanda) === "16") {
          setDataAccettazioneStep2(r.specifica || "");
          continue;
        }
        mappa[String(r.codice_domanda)] = {'''
          new = '''        if (q.codice === "ACCETTAZIONE_INCARICO" && String(r.codice_domanda) === "16") {
          setDataAccettazioneStep2(r.specifica || "");
          continue;
        }
        if (q.codice === "VALUTAZIONE_INDIPENDENZA") {
          const codice = String(r.codice_domanda);
          if (codice === "FEE_S") { setCompensoS(r.specifica || ""); continue; }
          if (codice === "FEE_C") { setCompensoC(r.specifica || ""); continue; }
          if (codice === "FEE_CT") { setCompensoCT(r.specifica || ""); continue; }
          if (codice === "SALVAGUARDIE") { setMisureSalvaguardia(r.specifica || ""); continue; }
          if (codice === "ESITO") { setEsitoIndipendenza(r.specifica || ""); continue; }
        }
        mappa[String(r.codice_domanda)] = {'''
          if old not in s: raise SystemExit('load risposta marker non trovato')
          s = s.replace(old, new, 1)

          old = '''      if (q.codice === "ACCETTAZIONE_INCARICO") {
        setRisposteStep2(mappa);
        setConclusioniStep2(q.conclusioni || "");
        setRischioIncarico(q.esito || "");
      }
    }'''
          new = '''      if (q.codice === "ACCETTAZIONE_INCARICO") {
        setRisposteStep2(mappa);
        setConclusioniStep2(q.conclusioni || "");
        setRischioIncarico(q.esito || "");
      }
      if (q.codice === "VALUTAZIONE_INDIPENDENZA") {
        setRisposteStep3(mappa);
        setConclusioniStep3(q.conclusioni || "");
        setRischioIndipendenza(q.esito || "");
      }
    }'''
          if old not in s: raise SystemExit('load q marker non trovato')
          s = s.replace(old, new, 1)

          old = '''  function aggiornaStep2(codice: string, patch: Partial<Riga>) {
    setRisposteStep2((p) => ({
      ...p,
      [codice]: { risposta: p[codice]?.risposta || "", specifica: p[codice]?.specifica || "", ...patch },
    }));
  }

  async function salvaBozza()'''
          new = '''  function aggiornaStep2(codice: string, patch: Partial<Riga>) {
    setRisposteStep2((p) => ({
      ...p,
      [codice]: { risposta: p[codice]?.risposta || "", specifica: p[codice]?.specifica || "", ...patch },
    }));
  }

  function aggiornaStep3(codice: string, patch: Partial<Riga>) {
    setRisposteStep3((p) => ({
      ...p,
      [codice]: { risposta: p[codice]?.risposta || "", specifica: p[codice]?.specifica || "", ...patch },
    }));
  }

  async function salvaBozza()'''
          if old not in s: raise SystemExit('update function marker non trovato')
          s = s.replace(old, new, 1)

          old = '''    if (step === 2) {
      const ok = await salvaQuestionario({
        supabase,
        preincaricoId: pre.id,
        codice: "ACCETTAZIONE_INCARICO",
        titolo: "Questionario accettazione incarico",
        domande: domandeStep2.map(([codice]) => codice),
        risposte: risposteStep2,
        conclusioni: conclusioniStep2,
        esito: rischioIncarico || null,
        completato: completateStep2 === domandeStep2.length && !!rischioIncarico,
        dataAccettazione: dataAccettazioneStep2,
      });
      if (!ok) return;
    }

    setMessaggio(`Step ${step} salvato correttamente.`);'''
          new = '''    if (step === 2) {
      const ok = await salvaQuestionario({
        supabase,
        preincaricoId: pre.id,
        codice: "ACCETTAZIONE_INCARICO",
        titolo: "Questionario accettazione incarico",
        domande: domandeStep2.map(([codice]) => codice),
        risposte: risposteStep2,
        conclusioni: conclusioniStep2,
        esito: rischioIncarico || null,
        completato: completateStep2 === domandeStep2.length && !!rischioIncarico,
        dataAccettazione: dataAccettazioneStep2,
      });
      if (!ok) return;
    }

    if (step === 3) {
      const ok = await salvaQuestionario({
        supabase,
        preincaricoId: pre.id,
        codice: "VALUTAZIONE_INDIPENDENZA",
        titolo: "Questionario valutazione indipendenza",
        domande: domandeStep3.map(([codice]) => codice),
        risposte: risposteStep3,
        conclusioni: conclusioniStep3,
        esito: rischioIndipendenza || null,
        completato: completateStep3 === domandeStep3.length && !!rischioIndipendenza && !!esitoIndipendenza,
        extraRighe: [
          { codice: "SALVAGUARDIE", specifica: misureSalvaguardia },
          { codice: "ESITO", specifica: esitoIndipendenza },
          { codice: "FEE_S", specifica: compensoS },
          { codice: "FEE_C", specifica: compensoC },
          { codice: "FEE_CT", specifica: compensoCT },
        ],
      });
      if (!ok) return;
    }

    setMessaggio(`Step ${step} salvato correttamente.`);'''
          if old not in s: raise SystemExit('save Step2 marker non trovato')
          s = s.replace(old, new, 1)

          old = '''    dataAccettazione?: string;
  }) {'''
          new = '''    dataAccettazione?: string;
    extraRighe?: Array<{ codice: string; specifica: string }>;
  }) {'''
          if old not in s: raise SystemExit('signature marker non trovato')
          s = s.replace(old, new, 1)

          old = '''    if (args.dataAccettazione) {
      righe.push({
        studio_id: studioId,
        questionario_id: q.id,
        codice_domanda: "16",
        risposta: null,
        specifica: args.dataAccettazione,
        ordine: 16,
        updated_at: new Date().toISOString(),
      });
    }

    if (righe.length) {'''
          new = '''    if (args.dataAccettazione) {
      righe.push({
        studio_id: studioId,
        questionario_id: q.id,
        codice_domanda: "16",
        risposta: null,
        specifica: args.dataAccettazione,
        ordine: 16,
        updated_at: new Date().toISOString(),
      });
    }

    for (const extra of args.extraRighe || []) {
      if (!extra.specifica) continue;
      righe.push({
        studio_id: studioId,
        questionario_id: q.id,
        codice_domanda: extra.codice,
        risposta: null,
        specifica: extra.specifica,
        ordine: righe.length + 1,
        updated_at: new Date().toISOString(),
      });
    }

    if (righe.length) {'''
          if old not in s: raise SystemExit('extra righe marker non trovato')
          s = s.replace(old, new, 1)

          old = '''    setRischioIncarico("");
    setDataAccettazioneStep2("");
    setMessaggio("");'''
          new = '''    setRischioIncarico("");
    setDataAccettazioneStep2("");
    setRisposteStep3({});
    setConclusioniStep3("");
    setRischioIndipendenza("");
    setEsitoIndipendenza("");
    setMisureSalvaguardia("");
    setCompensoS("");
    setCompensoC("");
    setCompensoCT("");
    setMessaggio("");'''
          if old not in s: raise SystemExit('reset marker non trovato')
          s = s.replace(old, new, 1)

          old = '''      {step > 2 && (
        <section style={card}>
          <h2 style={{ marginTop: 0 }}>Step {step} — {steps[step - 1]}</h2>
          <p style={{ color: "#64748b" }}>Modulo predisposto nel workflow. I quesiti del relativo documento saranno inseriti nel blocco dedicato.</p>
        </section>
      )}'''
          step3_ui = r'''      {step === 3 && (
        <section style={card}>
          <StepHeader title="Step 3 — Questionario valutazione indipendenza" completed={completateStep3} total={domandeStep3.length} />
          <div style={{ marginTop: 14, padding: 14, borderRadius: 10, background: "#fff7ed", border: "1px solid #fdba74", fontSize: 13, color: "#9a3412" }}>
            <b>Valutazione obbligatoria:</b> verificare incarichi ulteriori, rapporti economici e minacce all'indipendenza. Le risposte Sì/No/N.A. e le motivazioni restano archiviate nel fascicolo di presa in carico.
          </div>
          <div style={{ marginTop: 18 }}>
            {domandeStep3.map(([codice, sezione, testo, help], i) => {
              const nuovaSezione = i === 0 || domandeStep3[i - 1][1] !== sezione;
              return (
                <React.Fragment key={codice}>
                  {nuovaSezione && <div style={{ marginTop: i ? 24 : 0, padding: "10px 12px", borderRadius: 8, background: "#eaf8ff", color: "#075985", fontWeight: 900 }}>{sezione}</div>}
                  <QuestionRow codice={codice} testo={testo} help={help} riga={risposteStep3[codice]} onChange={(patch) => aggiornaStep3(codice, patch)} />
                </React.Fragment>
              );
            })}
          </div>

          <div style={{ marginTop: 22, padding: 16, borderRadius: 10, border: "1px solid #cbd5e1", background: "#f8fafc" }}>
            <h3 style={{ margin: "0 0 14px", fontSize: 17 }}>Compensi e dipendenza economica</h3>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 14 }}>
              <Field label="(S) Compensi per attività di revisione / sindaco">
                <input value={compensoS} onChange={(e) => setCompensoS(e.target.value)} inputMode="decimal" placeholder="0,00" style={input} />
              </Field>
              <Field label="(C) Altri compensi sulla società o sul gruppo">
                <input value={compensoC} onChange={(e) => setCompensoC(e.target.value)} inputMode="decimal" placeholder="0,00" style={input} />
              </Field>
              <Field label="(CT) Compensi totali professionista / rete">
                <input value={compensoCT} onChange={(e) => setCompensoCT(e.target.value)} inputMode="decimal" placeholder="0,00" style={input} />
              </Field>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 14, marginTop: 12 }}>
              <Mini label="(S + C) / CT" value={`${rapportoSCCT.toFixed(2)}%`} />
              <Mini label="S / (S + C)" value={rapportoSsuSC.toFixed(4)} />
            </div>
          </div>

          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 14, marginTop: 18 }}>
            <Field label="Rischio di indipendenza">
              <select value={rischioIndipendenza} onChange={(e) => setRischioIndipendenza(e.target.value)} style={input}>
                <option value="">Seleziona</option>
                <option value="BASSO">Basso</option>
                <option value="MEDIO">Medio</option>
                <option value="ALTO">Alto</option>
              </select>
            </Field>
            <Field label="Esito della valutazione">
              <select value={esitoIndipendenza} onChange={(e) => setEsitoIndipendenza(e.target.value)} style={input}>
                <option value="">Seleziona</option>
                <option value="ACCETTATO">Accettato</option>
                <option value="RIFIUTATO">Rifiutato</option>
              </select>
            </Field>
          </div>
          <div style={{ marginTop: 16 }}>
            <Field label="Misure di salvaguardia">
              <textarea value={misureSalvaguardia} onChange={(e) => setMisureSalvaguardia(e.target.value)} placeholder="Descrivere le misure previste per eliminare o ridurre le minacce all'indipendenza a un livello accettabile..." style={{ ...input, minHeight: 120, resize: "vertical" }} />
            </Field>
          </div>
          <div style={{ marginTop: 16 }}>
            <Field label="Conclusioni sulla valutazione di indipendenza">
              <textarea value={conclusioniStep3} onChange={(e) => setConclusioniStep3(e.target.value)} placeholder="Riportare le conclusioni finali e gli elementi acquisiti a supporto dell'accettazione o del rifiuto..." style={{ ...input, minHeight: 140, resize: "vertical" }} />
            </Field>
          </div>
        </section>
      )}

      {step > 3 && (
        <section style={card}>
          <h2 style={{ marginTop: 0 }}>Step {step} — {steps[step - 1]}</h2>
          <p style={{ color: "#64748b" }}>Modulo predisposto nel workflow. I quesiti del relativo documento saranno inseriti nel blocco dedicato.</p>
        </section>
      )}'''
          if old not in s: raise SystemExit('placeholder marker non trovato')
          s = s.replace(old, step3_ui, 1)

          p.write_text(s)
          PY

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/revisione-controllo/presa-in-carico.tsx
          git commit -m "Add audit independence questionnaire workflow"
          git push origin HEAD:main
